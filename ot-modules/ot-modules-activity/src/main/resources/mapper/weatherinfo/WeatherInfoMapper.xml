<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.onethinker.weatherinfo.mapper.WeatherInfoMapper">

    <resultMap type="WeatherInfo" id="WeatherInfoResult">
            <result property="id" column="id"/>
            <result property="createTime" column="create_time"/>
            <result property="updateTime" column="update_time"/>
            <result property="createBy" column="create_by"/>
            <result property="updateBy" column="update_by"/>
            <result property="enabled" column="enabled"/>
            <result property="weight" column="weight"/>
            <result property="key" column="key"/>
            <result property="city" column="city"/>
            <result property="extensions" column="extensions"/>
            <result property="sendResult" column="send_result"/>
    </resultMap>

    <sql id="selectWeatherInfoVo">
        select id, create_time, update_time, create_by, update_by, enabled, weight, key, city, extensions, send_result
        from t_weather_info
    </sql>

    <select id="selectWeatherInfoList" parameterType="WeatherInfo" resultMap="WeatherInfoResult">
        <include refid="selectWeatherInfoVo"/>
        <where>
                        <if test="enabled != null ">
                            and enabled = #{enabled}
                        </if>
                        <if test="weight != null ">
                            and weight = #{weight}
                        </if>
                        <if test="key != null  and key != ''">
                            and key = #{key}
                        </if>
                        <if test="city != null  and city != ''">
                            and city = #{city}
                        </if>
                        <if test="extensions != null  and extensions != ''">
                            and extensions = #{extensions}
                        </if>
                        <if test="sendResult != null  and sendResult != ''">
                            and send_result = #{sendResult}
                        </if>
        </where>
    </select>

    <select id="selectWeatherInfoById" parameterType="Long"
            resultMap="WeatherInfoResult">
            <include refid="selectWeatherInfoVo"/>
            where id = #{id}
    </select>

    <insert id="insertWeatherInfo" parameterType="WeatherInfo" useGeneratedKeys="true"
            keyProperty="id">
        insert into t_weather_info
        <trim prefix="(" suffix=")" suffixOverrides=",">
                    <if test="createTime != null">create_time,
                    </if>
                    <if test="updateTime != null">update_time,
                    </if>
                    <if test="createBy != null">create_by,
                    </if>
                    <if test="updateBy != null">update_by,
                    </if>
                    <if test="enabled != null">enabled,
                    </if>
                    <if test="weight != null">weight,
                    </if>
                    <if test="key != null and key != ''">key,
                    </if>
                    <if test="city != null and city != ''">city,
                    </if>
                    <if test="extensions != null and extensions != ''">extensions,
                    </if>
                    <if test="sendResult != null">send_result,
                    </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
                    <if test="createTime != null">#{createTime},
                    </if>
                    <if test="updateTime != null">#{updateTime},
                    </if>
                    <if test="createBy != null">#{createBy},
                    </if>
                    <if test="updateBy != null">#{updateBy},
                    </if>
                    <if test="enabled != null">#{enabled},
                    </if>
                    <if test="weight != null">#{weight},
                    </if>
                    <if test="key != null and key != ''">#{key},
                    </if>
                    <if test="city != null and city != ''">#{city},
                    </if>
                    <if test="extensions != null and extensions != ''">#{extensions},
                    </if>
                    <if test="sendResult != null">#{sendResult},
                    </if>
        </trim>
    </insert>

    <update id="updateWeatherInfo" parameterType="WeatherInfo">
        update t_weather_info
        <trim prefix="SET" suffixOverrides=",">
                    <if test="createTime != null">create_time =
                        #{createTime},
                    </if>
                    <if test="updateTime != null">update_time =
                        #{updateTime},
                    </if>
                    <if test="createBy != null">create_by =
                        #{createBy},
                    </if>
                    <if test="updateBy != null">update_by =
                        #{updateBy},
                    </if>
                    <if test="enabled != null">enabled =
                        #{enabled},
                    </if>
                    <if test="weight != null">weight =
                        #{weight},
                    </if>
                    <if test="key != null and key != ''">key =
                        #{key},
                    </if>
                    <if test="city != null and city != ''">city =
                        #{city},
                    </if>
                    <if test="extensions != null and extensions != ''">extensions =
                        #{extensions},
                    </if>
                    <if test="sendResult != null">send_result =
                        #{sendResult},
                    </if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteWeatherInfoById" parameterType="Long">
        delete from t_weather_info where id = #{id}
    </delete>

    <delete id="deleteWeatherInfoByIds" parameterType="String">
        delete from t_weather_info where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
