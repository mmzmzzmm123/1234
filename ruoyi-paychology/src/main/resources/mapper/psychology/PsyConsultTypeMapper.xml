<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.psychology.mapper.PsyConsultTypeMapper">
    
    <resultMap type="PsyConsultType" id="PsyConsultTypeResult">
        <result property="id"    column="id"    />
        <result property="pid"    column="pid"    />
        <result property="path"    column="path"    />
        <result property="name"    column="name"    />
        <result property="type"    column="type"    />
        <result property="sort"    column="sort"    />
        <result property="delFlag"    column="del_flag"    />
        <result property="status"    column="status"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <sql id="selectPsyConsultTypeVo">
        select id, pid, path, name, type, sort, del_flag, status, create_by, create_time, update_by, update_time from psy_consult_type
    </sql>

    <select id="selectPsyConsultTypeList" parameterType="PsyConsultType" resultMap="PsyConsultTypeResult">
        <include refid="selectPsyConsultTypeVo"/>
        <where>  
            <if test="pid != null "> and pid = #{pid}</if>
            <if test="path != null  and path != ''"> and path = #{path}</if>
            <if test="name != null  and name != ''"> and name like concat('%', #{name}, '%')</if>
            <if test="type != null  and type != ''"> and type = #{type}</if>
            <if test="sort != null "> and sort = #{sort}</if>
            <if test="status != null  and status != ''"> and status = #{status}</if>
        </where>
        order by sort desc
    </select>
    
    <select id="selectPsyConsultTypeById" parameterType="Long" resultMap="PsyConsultTypeResult">
        <include refid="selectPsyConsultTypeVo"/>
        where id = #{id}
    </select>

    <update id="tombstonedByIds">
        update psy_consult_serve_config set del_flag = 1 where id in
        <foreach collection="array" item="item" open="(" separator="," close=")">
            #{item,jdbcType=BIGINT}
        </foreach>
    </update>
</mapper>