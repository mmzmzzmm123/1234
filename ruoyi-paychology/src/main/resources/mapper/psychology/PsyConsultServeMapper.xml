<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.psychology.mapper.PsyConsultServeMapper">

    <update id="tombstonedByIds">
        update psy_consult_serve set del_flag = 1 where id in
        <foreach collection="array" item="item" open="(" separator="," close=")">
            #{item,jdbcType=BIGINT}
        </foreach>
    </update>

    <resultMap type="PsyConsultServe" id="PsyConsultServeResult">
        <result property="id"    column="id"    />
        <result property="consultId"    column="consult_id"    />
        <result property="name"    column="name"    />
        <result property="info"    column="info"    />
        <result property="price"    column="price"    />
        <result property="delFlag"    column="del_flag"    />
        <result property="status"    column="status"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <sql id="selectPsyConsultServeVo">
        select id, consult_id, name, info, price, del_flag, status, create_by, create_time, update_by, update_time from psy_consult_serve
    </sql>

    <select id="getList" parameterType="PsyConsultServe" resultMap="PsyConsultServeResult">
        <include refid="selectPsyConsultServeVo"/>
        <where>
            <if test="consultId != null "> and consult_id = #{consultId}</if>
            <if test="name != null  and name != ''"> and name like concat('%', #{name}, '%')</if>
            <if test="info != null  and info != ''"> and info = #{info}</if>
            <if test="price != null "> and price = #{price}</if>
            <if test="status != null  and status != ''"> and status = #{status}</if>
        </where>
    </select>
</mapper>