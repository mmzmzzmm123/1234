<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.onethinker.onethinker.mapper.BannerMapper">

    <resultMap type="Banner" id="BannerResult">
            <result property="id" column="id"/>
            <result property="createTime" column="create_time"/>
            <result property="updateTime" column="update_time"/>
            <result property="enabled" column="enabled"/>
            <result property="weight" column="weight"/>
            <result property="fileId" column="file_id"/>
            <result property="url" column="url"/>
            <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectBannerVo">
        select id, create_time, update_time, enabled, weight, file_id, url, remark
        from t_banner
    </sql>

    <select id="selectBannerList" parameterType="Banner" resultMap="BannerResult">
        <include refid="selectBannerVo"/>
        <where>
                        <if test="enabled != null ">
                            and enabled = #{enabled}
                        </if>
                        <if test="weight != null ">
                            and weight = #{weight}
                        </if>
                        <if test="fileId != null  and fileId != ''">
                            and file_id = #{fileId}
                        </if>
                        <if test="url != null  and url != ''">
                            and url = #{url}
                        </if>
        </where>
    </select>

    <select id="selectBannerById" parameterType="Long"
            resultMap="BannerResult">
            <include refid="selectBannerVo"/>
            where id = #{id}
    </select>

    <insert id="insertBanner" parameterType="Banner" useGeneratedKeys="true"
            keyProperty="id">
        insert into t_banner
        <trim prefix="(" suffix=")" suffixOverrides=",">
                    <if test="createTime != null">create_time,
                    </if>
                    <if test="updateTime != null">update_time,
                    </if>
                    <if test="enabled != null">enabled,
                    </if>
                    <if test="weight != null">weight,
                    </if>
                    <if test="fileId != null">file_id,
                    </if>
                    <if test="url != null">url,
                    </if>
                    <if test="remark != null">remark,
                    </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
                    <if test="createTime != null">#{createTime},
                    </if>
                    <if test="updateTime != null">#{updateTime},
                    </if>
                    <if test="enabled != null">#{enabled},
                    </if>
                    <if test="weight != null">#{weight},
                    </if>
                    <if test="fileId != null">#{fileId},
                    </if>
                    <if test="url != null">#{url},
                    </if>
                    <if test="remark != null">#{remark},
                    </if>
        </trim>
    </insert>

    <update id="updateBanner" parameterType="Banner">
        update t_banner
        <trim prefix="SET" suffixOverrides=",">
                    <if test="createTime != null">create_time =
                        #{createTime},
                    </if>
                    <if test="updateTime != null">update_time =
                        #{updateTime},
                    </if>
                    <if test="enabled != null">enabled =
                        #{enabled},
                    </if>
                    <if test="weight != null">weight =
                        #{weight},
                    </if>
                    <if test="fileId != null">file_id =
                        #{fileId},
                    </if>
                    <if test="url != null">url =
                        #{url},
                    </if>
                    <if test="remark != null">remark =
                        #{remark},
                    </if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteBannerById" parameterType="Long">
        delete from t_banner where id = #{id}
    </delete>

    <delete id="deleteBannerByIds" parameterType="String">
        delete from t_banner where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
