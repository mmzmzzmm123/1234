<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.RobotMapper">
    <select id="selectRobotPageList" resultType="com.ruoyi.system.domain.vo.robot.SelectRobotListVO">
        select *
        from t_robot_info r
        where 1=1
        <if test="dto.startTime != null and dto.startTime != ''">
            and t.create_time >= #{dto.startTime}
        </if>
        <if test="dto.endTime != null and dto.endTime != ''">
            and t.create_time &lt; #{dto.endTime}
        </if>
        <if test="dto.type != null">
            and t.type = #{dto.type}
        </if>
        <if test="dto.tgAppVersion != null and dto.tgAppVersion != ''">
            and t.tg_app_version = #{dto.tgAppVersion}
        </if>
        <if test="dto.heartbeatStatus != null">
            and t.heartbeat_status = #{dto.heartbeatStatus}
        </if>
        <if test="dto.sealStatus != null">
            and t.seal_status = #{dto.sealStatus}
        </if>
        <if test="dto.bidirectionalType != null">
            and t.bidirectional_type = #{dto.bidirectionalType}
        </if>
        <if test="dto.country != null and dto.country != ''">
            and t.country like  concat('%',#{dto.country},'%')
        </if>
        <if test="dto.occupyType != null">
            and t.occupy_type = #{dto.occupyType}
        </if>
        <if test="dto.enableType != null">
            and t.enable_type = #{dto.enableType]}
        </if>
        <if test="dto.groupOwner != null">
            and t.group_owner = #{dto.groupOwner}
        </if>
        <if test="dto.name != null and dto.name != ''">
            and concat(t.first_name,' ',t.last_name) like concat('%',#{dto.name},'%')
        </if>
        <if test="dto.phone != null and dto.phone != ''">
            and t.phone like concat('%',#{dto.phone},'%')
        </if>
        <if test="dto.userName != null and dto.userName != ''">
            and t.user_name like concat('%',#{dto.userName},'%')
        </if>
        <if test="dto.robotSerialNos != null and dto.robotSerialNos.size > 0">
            and t.robot_serial_no in
        <foreach collection="dto.robotSerialNos" item="item" separator="," open="(" close=")">
            #{item}
        </foreach>
        </if>

    </select>

</mapper>
