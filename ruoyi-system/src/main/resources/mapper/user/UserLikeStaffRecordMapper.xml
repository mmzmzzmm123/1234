<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.user.mapper.UserLikeStaffRecordMapper">

    <resultMap type="UserLikeStaffRecord" id="UserLikeStaffRecordResult">
        <result property="id"    column="id"    />
        <result property="userId"    column="user_id"    />
        <result property="staffUserId"    column="staff_user_id"    />
        <result property="createTime"    column="create_time"    />
    </resultMap>

    <sql id="selectUserLikeStaffRecordVo">
        select id, user_id, staff_user_id, create_time from bus_user_like_staff_record
    </sql>

    <select id="selectUserLikeStaffRecordList" parameterType="UserLikeStaffRecord" resultMap="UserLikeStaffRecordResult">
        <include refid="selectUserLikeStaffRecordVo"/>
        <where>
            <if test="userId != null "> and user_id = #{userId}</if>
            <if test="staffUserId != null "> and staff_user_id = #{staffUserId}</if>
        </where>
    </select>

    <select id="selectStaffUserIdByUserId" parameterType="Long" resultType="Long">
        select staff_user_id from bus_user_like_staff_record where user_id = #{userId}
    </select>

    <select id="selectUserLikeStaffRecordById" parameterType="Long" resultMap="UserLikeStaffRecordResult">
        <include refid="selectUserLikeStaffRecordVo"/>
        where id = #{id}
    </select>

    <insert id="insertUserLikeStaffRecord" parameterType="UserLikeStaffRecord" useGeneratedKeys="true" keyProperty="id">
        insert into bus_user_like_staff_record
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userId != null">user_id,</if>
            <if test="staffUserId != null">staff_user_id,</if>
            <if test="createTime != null">create_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="userId != null">#{userId},</if>
            <if test="staffUserId != null">#{staffUserId},</if>
            <if test="createTime != null">#{createTime},</if>
         </trim>
    </insert>

    <update id="updateUserLikeStaffRecord" parameterType="UserLikeStaffRecord">
        update bus_user_like_staff_record
        <trim prefix="SET" suffixOverrides=",">
            <if test="userId != null">user_id = #{userId},</if>
            <if test="staffUserId != null">staff_user_id = #{staffUserId},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteUserLikeStaffRecordById" parameterType="Long">
        delete from bus_user_like_staff_record where id = #{id}
    </delete>

    <delete id="deleteUserLikeStaffRecordByIds" parameterType="String">
        delete from bus_user_like_staff_record where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
