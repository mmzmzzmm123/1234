<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.BusWalletsMapper">
    
    <resultMap type="BusWallets" id="BusWalletsResult">
        <result property="walletId"    column="wallet_id"    />
        <result property="userId"    column="user_id"    />
        <result property="balance"    column="balance"    />
        <result property="frozenBalance"    column="frozen_balance"    />
        <result property="version"    column="version"    />
        <result property="updatedAt"    column="updated_at"    />
        <result property="createdAt"    column="created_at"    />
    </resultMap>

    <sql id="selectBusWalletsVo">
        select wallet_id, user_id, balance, frozen_balance, version, updated_at, created_at from bus_wallets
    </sql>

    <select id="selectBusWalletsList" parameterType="BusWallets" resultMap="BusWalletsResult">
        <include refid="selectBusWalletsVo"/>
        <where>  
            <if test="userId != null "> and user_id = #{userId}</if>
            <if test="balance != null "> and balance = #{balance}</if>
            <if test="frozenBalance != null "> and frozen_balance = #{frozenBalance}</if>
            <if test="version != null "> and version = #{version}</if>
            <if test="updatedAt != null "> and updated_at = #{updatedAt}</if>
            <if test="createdAt != null "> and created_at = #{createdAt}</if>
        </where>
    </select>
    
    <select id="selectBusWalletsByWalletId" parameterType="Long" resultMap="BusWalletsResult">
        <include refid="selectBusWalletsVo"/>
        where wallet_id = #{walletId}
    </select>

    <insert id="insertBusWallets" parameterType="BusWallets" useGeneratedKeys="true" keyProperty="walletId">
        insert into bus_wallets
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userId != null">user_id,</if>
            <if test="balance != null">balance,</if>
            <if test="frozenBalance != null">frozen_balance,</if>
            <if test="version != null">version,</if>
            <if test="updatedAt != null">updated_at,</if>
            <if test="createdAt != null">created_at,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="userId != null">#{userId},</if>
            <if test="balance != null">#{balance},</if>
            <if test="frozenBalance != null">#{frozenBalance},</if>
            <if test="version != null">#{version},</if>
            <if test="updatedAt != null">#{updatedAt},</if>
            <if test="createdAt != null">#{createdAt},</if>
         </trim>
    </insert>

    <update id="updateBusWallets" parameterType="BusWallets">
        update bus_wallets
        <trim prefix="SET" suffixOverrides=",">
            <if test="userId != null">user_id = #{userId},</if>
            <if test="balance != null">balance = #{balance},</if>
            <if test="frozenBalance != null">frozen_balance = #{frozenBalance},</if>
            <if test="version != null">version = #{version},</if>
            <if test="updatedAt != null">updated_at = #{updatedAt},</if>
            <if test="createdAt != null">created_at = #{createdAt},</if>
        </trim>
        where wallet_id = #{walletId}
    </update>

    <delete id="deleteBusWalletsByWalletId" parameterType="Long">
        delete from bus_wallets where wallet_id = #{walletId}
    </delete>

    <delete id="deleteBusWalletsByWalletIds" parameterType="String">
        delete from bus_wallets where wallet_id in 
        <foreach item="walletId" collection="array" open="(" separator="," close=")">
            #{walletId}
        </foreach>
    </delete>
</mapper>